file(GLOB SOURCES "*.cpp")
file(GLOB_RECURSE HEADERS "*.h")
include_directories(${CMAKE_CURRENT_SOURCE_DIR} "/include")
include_directories(${CMAKE_CURRENT_SOURCE_DIR} "/external/Eigen")
add_executable(vortex ${SOURCES} ${HEADERS})